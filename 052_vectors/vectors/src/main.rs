fn main() {

    // array q da pra mudar de tamanhp
    let test = vec![1,2,3,4,5];
    let mut words = vec!["a", "b", "c"];
    words.push("d");
    words.push("e");

    // add ao final do vetor

    words.insert(0, "0");

    // elimina do final do vetor e passa pra essa variavel a letra e
    let last_letter = words.pop();

    // vai tirar o "0"
    words.remove(0);
    // com tamanho fixo

    
    let array = ["a", "b"];

    
}
